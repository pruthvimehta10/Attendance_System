# Copy to .env and set secure values before deploying
SECRET_KEY=replace-me-with-secure-random
# Example for sqlite (default):
DATABASE_URL=sqlite:///attendance.db
# Or for Postgres on cloud: postgresql://user:pass@host:port/dbname
